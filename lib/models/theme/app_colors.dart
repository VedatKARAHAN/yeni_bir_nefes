import 'package:flutter/material.dart';

class AppColors {
  //AppBar,BottomNavigator,butonların arka planı!
  static const Color primary = Color(0xFF00BFA5);

  //sekmeler
  static const Color secondary = Color(0xFF80DEEA);

  static const Color background = Color(0xFFE0F7FA);

  //button kenarları ve önemliler
  static const Color accent = Color(0xFFFF80AB);

  static const Color textDark = Color(0xFF212121);

  static const Color textLight = Colors.white;
}
